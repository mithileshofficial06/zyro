{
  "name": "@zyro/subgraph",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "fixtures": "node scripts/generate-fixtures.mjs",
    "codegen": "graph codegen",
    "build": "graph build --network base-sepolia",
    "test": "graph test",
    "check": "npm run fixtures && npm run codegen && npm run build",
    "deploy": "graph deploy zyro --network base-sepolia",
    "auth": "graph auth"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.1",
    "matchstick-as": "^0.6.0"
  }
}
